let name = "John";
let price = 19.99;
let bigNumber = BigInt(123456789012345678901234567890);
let hasPermission = false;
let result = null;
let user; 
let colors = ["red", "green", "blue"];
let person = {
  name: "John",
  age: 30,
  isStudent: false,
};

console.log (`Type of ${name} is ${typeof name}`);
console.log (`Type of ${price} is ${typeof price}`);
console.log (`Type of ${bigNumber} is ${typeof bigNumber}`);
console.log (`Type of ${hasPermission} is ${typeof hasPermission}`);
console.log (`Type of ${user} is ${typeof user}`);
console.log (`Type of ${result} is ${typeof result}`);
console.log (`Type of ${colors} is ${typeof colors}`);
console.log (`Type of ${person} is ${typeof person}`);